import { useState } from "react";
import "../styles/chanel_style.module.css";

const ListChanel = () => {
    const [list_chanel,SetListChanel] = useState([]);

    return(
    <div id="container">
        <div id="chanel">
        {/**   {
            list_chanel.map((e)=>{
                
            })
            }
            */  }
        </div>
    </div>
    )
}

export default ListChanel;