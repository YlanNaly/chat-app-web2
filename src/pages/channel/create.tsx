/**
 * export async functin getServerSideProps(){
 *  const req = await axios.post(...);
 * 
 * const data = await req.json();
 * 
 * const preprocessData = (data:any[]) => {
 *      const processedChannel: any [] = []
 * }
 * }
 */